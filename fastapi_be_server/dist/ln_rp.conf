upstream service {
    server 10.0.0.201:3000;
}

upstream partner {
    server 10.0.0.201:3001;
}

upstream cms {
    server 10.0.0.201:3002;
}

upstream api {
    server 10.0.100.110:3010;
}

upstream search {
    server 10.0.100.110:7700;
}

upstream auth {
    server 10.0.100.110:8080;
}

# 일반 사이트(service, www)
server {
    listen 443 ssl default_server;

    # 도메인
    server_name www.likenovel.net likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://service;
    }
}

# 파트너 사이트(partner)
server {
    listen 443 ssl;

    # 도메인
    server_name partner.likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://partner;
    }
}

# 관리자 사이트(cms)
server {
    listen 443 ssl;

    # 도메인
    server_name cms.likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://cms;
    }
}

# FastAPI 백엔드(api)
server {
    listen 443 ssl;

    # 도메인
    server_name api.likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://api;

        # cloudflare restoring original visitor IPs
        set_real_ip_from 173.245.48.0/20;
        set_real_ip_from 103.21.244.0/22;
        set_real_ip_from 103.22.200.0/22;
        set_real_ip_from 103.31.4.0/22;
        set_real_ip_from 141.101.64.0/18;
        set_real_ip_from 108.162.192.0/18;
        set_real_ip_from 190.93.240.0/20;
        set_real_ip_from 188.114.96.0/20;
        set_real_ip_from 197.234.240.0/22;
        set_real_ip_from 198.41.128.0/17;
        set_real_ip_from 162.158.0.0/15;
        set_real_ip_from 104.16.0.0/13;
        set_real_ip_from 104.24.0.0/14;
        set_real_ip_from 172.64.0.0/13;
        set_real_ip_from 131.0.72.0/22;
        set_real_ip_from 2400:cb00::/32;
        set_real_ip_from 2606:4700::/32;
        set_real_ip_from 2803:f800::/32;
        set_real_ip_from 2405:b500::/32;
        set_real_ip_from 2405:8100::/32;
        set_real_ip_from 2a06:98c0::/29;
        set_real_ip_from 2c0f:f248::/32;

        real_ip_header CF-Connecting-IP;
    }
}

# Meilisearch(search)
server {
    listen 443 ssl;

    # 도메인
    server_name search.likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://search;
    }
}

# Keycloak(auth)
server {
    listen 443 ssl;

    # 도메인
    server_name auth.likenovel.net;

    # ssl
    ssl_certificate /home/ln-admin/.ssh/cf_ssl.crt;
    ssl_certificate_key /home/ln-admin/.ssh/cf_ssl.pem;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://auth;
    }
}

